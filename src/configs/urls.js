const baseURL = 'https://jsonplaceholder.typicode.com';

export const urls = {
  users: '/users',
  posts: '/posts',
  comments: '/comments',
  albums: '/albums',
  photos: '/photos'
}

export default baseURL;